<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <title>Wild Atlantic Bikers - WAW Motorbike Blog</title>
</head>
<body>
    <% if (sessionFlash) { %>
      <div class="alert alert-<%- sessionFlash.type %>">
        <strong><%- sessionFlash.type %></strong> <%- sessionFlash.message %>
      </div>
    <% } %>
    <div class="container">
        <h1 class="mb-1 mt-3"><%= article.title %></h1>
        <div class="text-muted mb-2"><%= article.createdAt.toLocaleDateString() %></div>
        <div class="text-muted mb-2">Article by <%= article.author.username %></div>
        <div class="mb-4">
            <a href="/" class="btn btn-secondary">All Articles</a>
            <% if (isLoggedIn) { %>
                <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
            <% } %>
        </div>
        <div class="card">
            <div class="card-body">
                <%- article.sanitizedHtml %>
            </div>
        </div>
    </div>
</body>
</html>